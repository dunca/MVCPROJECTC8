@model IEnumerable<MvcProjectC8.Models.ProjectEvaluation>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Evaluation/EvaluationLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@section mySection{
Total no of evaluations is @Model.Count();
}

@foreach (var item in Model)
{
    @Html.Partial("~/Views/Evaluation/_Evaluation.cshtml", item);
}
